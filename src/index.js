// This index.js file is no longer needed since each block is built separately
// with a separate entry point for each block.
// See webpack.config.js

import './starting-block';
import './second-block';
